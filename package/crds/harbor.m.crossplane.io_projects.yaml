---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: projects.harbor.m.crossplane.io
spec:
  group: harbor.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harbor
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Project is the Schema for the Projects API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectSpec defines the desired state of Project
            properties:
              forProvider:
                properties:
                  autoSbomGeneration:
                    description: '(Boolean) Automatically generate SBOM for images
                      pushed to this project. (Default: false) can only be used with
                      Harbor version v2.11.0 and above'
                    type: boolean
                  cveAllowlist:
                    description: 123", "CVE-145"] or ["CVE-123"]
                    items:
                      type: string
                    type: array
                  deploymentSecurity:
                    description: '(String) Prevent deployment of images with vulnerability
                      severity equal or higher than the specified value. Images must
                      be scanned before this takes effect. Possible values: "critical",
                      "high", "medium", "low", "none". (Default: null)'
                    type: string
                  enableContentTrust:
                    description: '(Boolean) Enables Content Trust for project. When
                      enabled it queries the embedded docker notary server. (Default:
                      false).'
                    type: boolean
                  enableContentTrustCosign:
                    description: '(Boolean) Enables Content Trust Cosign for project.
                      When enabled it queries Cosign. (Default: false)'
                    type: boolean
                  forceDestroy:
                    description: (Boolean) A boolean that indicates all repositories
                      should be deleted from the project so that the project can be
                      destroyed without error. These repositories are not recoverable.
                    type: boolean
                  proxySpeedKb:
                    description: 1)
                    format: int64
                    type: integer
                  public:
                    description: '(Boolean) The project will be public accessibility.(Default:
                      false)'
                    type: boolean
                  registryId:
                    description: (Number) To enable project as Proxy Cache.
                    format: int64
                    type: integer
                  storageQuota:
                    description: (Number) The storage quota of the project in GB's.
                    format: int64
                    type: integer
                  vulnerabilityScanner:
                    description: (String) The name of the vulnerability scanner to
                      use for this project, overriding the global default scanner.
                      If not set, the project uses the global default scanner configured
                      via harbor_interrogation_services.
                    type: string
                  vulnerabilityScanning:
                    description: '(Boolean) Images will be scanned for vulnerabilities
                      when push to harbor. (Default: true)'
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoSbomGeneration:
                    description: '(Boolean) Automatically generate SBOM for images
                      pushed to this project. (Default: false) can only be used with
                      Harbor version v2.11.0 and above'
                    type: boolean
                  cveAllowlist:
                    description: 123", "CVE-145"] or ["CVE-123"]
                    items:
                      type: string
                    type: array
                  deploymentSecurity:
                    description: '(String) Prevent deployment of images with vulnerability
                      severity equal or higher than the specified value. Images must
                      be scanned before this takes effect. Possible values: "critical",
                      "high", "medium", "low", "none". (Default: null)'
                    type: string
                  enableContentTrust:
                    description: '(Boolean) Enables Content Trust for project. When
                      enabled it queries the embedded docker notary server. (Default:
                      false).'
                    type: boolean
                  enableContentTrustCosign:
                    description: '(Boolean) Enables Content Trust Cosign for project.
                      When enabled it queries Cosign. (Default: false)'
                    type: boolean
                  forceDestroy:
                    description: (Boolean) A boolean that indicates all repositories
                      should be deleted from the project so that the project can be
                      destroyed without error. These repositories are not recoverable.
                    type: boolean
                  proxySpeedKb:
                    description: 1)
                    format: int64
                    type: integer
                  public:
                    description: '(Boolean) The project will be public accessibility.(Default:
                      false)'
                    type: boolean
                  registryId:
                    description: (Number) To enable project as Proxy Cache.
                    format: int64
                    type: integer
                  storageQuota:
                    description: (Number) The storage quota of the project in GB's.
                    format: int64
                    type: integer
                  vulnerabilityScanner:
                    description: (String) The name of the vulnerability scanner to
                      use for this project, overriding the global default scanner.
                      If not set, the project uses the global default scanner configured
                      via harbor_interrogation_services.
                    type: string
                  vulnerabilityScanning:
                    description: '(Boolean) Images will be scanned for vulnerabilities
                      when push to harbor. (Default: true)'
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ProjectStatus defines the observed state of Project.
            properties:
              atProvider:
                properties:
                  autoSbomGeneration:
                    description: '(Boolean) Automatically generate SBOM for images
                      pushed to this project. (Default: false) can only be used with
                      Harbor version v2.11.0 and above'
                    type: boolean
                  cveAllowlist:
                    description: 123", "CVE-145"] or ["CVE-123"]
                    items:
                      type: string
                    type: array
                  deploymentSecurity:
                    description: '(String) Prevent deployment of images with vulnerability
                      severity equal or higher than the specified value. Images must
                      be scanned before this takes effect. Possible values: "critical",
                      "high", "medium", "low", "none". (Default: null)'
                    type: string
                  enableContentTrust:
                    description: '(Boolean) Enables Content Trust for project. When
                      enabled it queries the embedded docker notary server. (Default:
                      false).'
                    type: boolean
                  enableContentTrustCosign:
                    description: '(Boolean) Enables Content Trust Cosign for project.
                      When enabled it queries Cosign. (Default: false)'
                    type: boolean
                  forceDestroy:
                    description: (Boolean) A boolean that indicates all repositories
                      should be deleted from the project so that the project can be
                      destroyed without error. These repositories are not recoverable.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  projectId:
                    description: (Number) The project id of this resource.
                    format: int64
                    type: integer
                  proxySpeedKb:
                    description: 1)
                    format: int64
                    type: integer
                  public:
                    description: '(Boolean) The project will be public accessibility.(Default:
                      false)'
                    type: boolean
                  registryId:
                    description: (Number) To enable project as Proxy Cache.
                    format: int64
                    type: integer
                  storageQuota:
                    description: (Number) The storage quota of the project in GB's.
                    format: int64
                    type: integer
                  vulnerabilityScanner:
                    description: (String) The name of the vulnerability scanner to
                      use for this project, overriding the global default scanner.
                      If not set, the project uses the global default scanner configured
                      via harbor_interrogation_services.
                    type: string
                  vulnerabilityScanning:
                    description: '(Boolean) Images will be scanned for vulnerabilities
                      when push to harbor. (Default: true)'
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
